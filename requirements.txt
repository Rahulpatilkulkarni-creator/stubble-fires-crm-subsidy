# ---- Core (installed now) ----
pandas>=2.0
numpy>=1.24
requests>=2.31
python-dotenv>=1.0
tqdm>=4.66

# ---- Geospatial (Phase 2: district aggregation & maps) ----
geopandas>=0.14
shapely>=2.0
pyogrio>=0.7
folium>=0.15

# ---- Visualization ----
matplotlib>=3.7
seaborn>=0.13

# ---- Modeling (Phase 3–4: causal + prediction) ----
scikit-learn>=1.3
lightgbm>=4.0
statsmodels>=0.14
linearmodels>=5.3      # PanelOLS / two-way fixed effects for DiD

# ---- Notebooks ----
jupyter>=1.0
ipykernel>=6.25
nbclient>=0.9          # deterministic notebook execution in _build_notebooks.py
nbformat>=5.9

# ---- Dashboard (app/streamlit_app.py) ----
streamlit>=1.40
plotly>=5.24           # choropleth_map (maplibre) for the hotspot map

